Import('env')

env['LIBS'] = ['m']
#env['CFLAGS'] = ['-O2']

source = [
    'graph7.c',
    'csv_parser.c',
]

env.StaticLibrary(target='graph7-%s' % env['VERSION'], source=source)
#env.SharedLibrary(target = 'graph7', SHLIBVERSION = env['VERSION', source = ['graph7.c'])
